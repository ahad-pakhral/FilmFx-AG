---
phase: "06"
plan: "02"
type: "feature"
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt
  - app/src/main/java/com/filmfx/app/ui/EditorScreen.kt
  - app/src/main/java/com/filmfx/app/MainActivity.kt
autonomous: true
requirements: UI-02, UI-05
must_haves:
  truths:
    - "Editor state is managed centrally by EditorViewModel."
    - "UI extends edge-to-edge behind system bars."
    - "Effect parameters remain functional after migrating to ViewModel."
  artifacts:
    - path: "app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt"
      provides: "Central state management for editor"
  key_links:
    - from: "app/src/main/java/com/filmfx/app/ui/EditorScreen.kt"
      to: "app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt"
      via: "StateFlow collection"
---

# Plan 06-02: UI Refactoring & Immersive State
**Phase**: 06
**Goal**: Migrate state management to a ViewModel and implement the edge-to-edge immersive UI structure.

## Tasks

```xml
<tasks>
  <task id="1">
    <name>Create EditorViewModel</name>
    <description>Implement EditorViewModel to hold effect parameters and current project state.</description>
    <files>
      <file>app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt</file>
    </files>
    <action>Create ViewModel that manages a single source of truth for all film effect parameters.</action>
    <verify>./gradlew compileDebugKotlin</verify>
    <done>ViewModel compiles and holds the initial default state.</done>
  </task>
  <task id="2">
    <name>Immersive Layout Setup</name>
    <description>Update MainActivity and EditorScreen for immersive, edge-to-edge layout.</description>
    <files>
      <file>app/src/main/java/com/filmfx/app/MainActivity.kt</file>
      <file>app/src/main/java/com/filmfx/app/ui/EditorScreen.kt</file>
    </files>
    <action>Handle WindowInsets to remove top padding. UI should extend to the absolute top of the screen.</action>
    <verify>Manual: Deploy to device and check that Top Bar touches the screen top.</verify>
    <done>UI is truly immersive with no black bars or wasted space at the top.</done>
  </task>
  <task id="3">
    <name>Refactor State Wiring</name>
    <description>Refactor EditorScreen to use ViewModel state instead of local remember variables.</description>
    <files>
      <file>app/src/main/java/com/filmfx/app/ui/EditorScreen.kt</file>
    </files>
    <action>Wire effect parameters in EditorScreen to the ViewModel StateFlow.</action>
    <verify>Run app; verify sliders still work as before.</verify>
    <done>EditorScreen functionality is preserved after refactor.</done>
  </task>
</tasks>
```
