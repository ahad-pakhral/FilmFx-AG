---
phase: "06"
plan: "04"
type: "feature"
wave: 4
depends_on: ["06-03"]
files_modified:
  - app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt
  - app/src/main/java/com/filmfx/app/ui/EditorScreen.kt
autonomous: true
requirements: UI-04, UI-05
must_haves:
  truths:
    - "User can undo/redo parameter changes, including global reset."
    - "Long-press on preview shows original un-filtered image."
    - "App auto-saves current state to Room database with debounce."
  artifacts: []
  key_links:
    - from: "app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt"
      to: "app/src/main/java/com/filmfx/app/data/ProjectDao.kt"
      via: "Auto-save trigger"
---

# Plan 06-04: Editor Feature Completion
**Phase**: 06
**Goal**: Finalize the editor with Before/After comparison, Undo/Redo history, and auto-save persistence.

## Tasks

```xml
<tasks>
  <task id="1">
    <name>Undo/Redo Stack</name>
    <description>Implement Undo/Redo stack and Global Reset logic in EditorViewModel.</description>
    <files>
      <file>app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt</file>
    </files>
    <action>Create a history stack for parameter changes and a method to reset all parameters (with undo capability).</action>
    <verify>Manual: Edit parameters, reset all, then undo—confirm values are restored.</verify>
    <done>State history management is functional.</done>
  </task>
  <task id="2">
    <name>Before/After View</name>
    <description>Implement Before/After comparison (Long-press toggle) in EditorScreen.</description>
    <files>
      <file>app/src/main/java/com/filmfx/app/ui/EditorScreen.kt</file>
    </files>
    <action>Add a dedicated comparison trigger (or tap-hold on preview) that bypasses all filters in real-time.</action>
    <verify>Manual: Hold preview area—confirm image reverts to "original" look immediately.</verify>
    <done>User can easily compare current edits with the original photo.</done>
  </task>
  <task id="3">
    <name>Auto-Save Integration</name>
    <description>Hook up Auto-Save to Room database.</description>
    <files>
      <file>app/src/main/java/com/filmfx/app/ui/EditorViewModel.kt</file>
    </files>
    <action>Commit parameter changes to the Room database with a debounce timer to avoid excessive disk I/O.</action>
    <verify>Edit photo, restart app—confirm changes are exactly where they were left.</verify>
    <done>Persistence is transparent and reliable.</done>
  </task>
</tasks>
```
